<%- model_class = Campaign.new.class -%>
<br><br>
  <h1>My Campaigns</h1>
<table class="table table-striped">

  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
<td><%= link_to campaign.charity_id, campaign_path(campaign) %></td>
        <td>$300.00 raised</td>
        <td>30 days remaining</td>
        
        

        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_campaign_path(campaign), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      campaign_path(campaign),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_campaign_path,
            :class => 'btn btn-primary' %>


<br>
<br>
<br>
<br>


<h1>Friend's Campaigns</h1>
<table class="table table-striped">
  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
        <td><%= campaign.charity_id %></td>
        <td><%= campaign.owner_id %></td>
         <td>$300.00 raised</td>
        <td>30 days remaining</td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_campaign_path(campaign), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      campaign_path(campaign),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>
<br>
<br>
<br>
<h1>Top Campaigns</h1>
<table class="table table-striped">
  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
        <td><%= campaign.charity_id %></td>
        <td><%= campaign.owner_id %></td>
         <td>$300.00 raised</td>
        <td>30 days remaining</td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_campaign_path(campaign), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      campaign_path(campaign),
                      :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>